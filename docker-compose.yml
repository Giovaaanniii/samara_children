# Заготовка: позже добавить сервисы backend, frontend, postgres, redis, nginx.
#
# Пример:
# version: "3.9"
# services:
#   postgres:
#     image: postgres:16-alpine
#   redis:
#     image: redis:7-alpine
#   backend:
#     build: ./backend
#     ...
